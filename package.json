{
  "name": "friday-agents-monorepo",
  "private": true,
  "workspaces": [
    "app",
    "core"
  ],
  "scripts": {
    "build": "npm run build --workspace=core",
    "dev": "npm run dev --workspace=app"
  }
}
